# NTFS 工具 - AI 上下文记忆

> 目录：`/Users/jinniu/Desktop/Beanut-Mirror/Mirror/Trading-Ai/ntfs-tools/`
> 此文件为 AI 助手上下文记忆，后续在此目录下讨论 NTFS 相关问题。

---

## 环境信息

| 项目 | 值 |
|------|-----|
| Mac | MacBook Pro, Apple Silicon (M系列) |
| macOS | 26.5.2 (Tahoe) |
| macFUSE | 5.3.3 |
| ntfs-3g-mac | 2026.7.7 (gromgit/fuse tap) |
| 安装位置 | `/opt/homebrew/Cellar/ntfs-3g-mac/2026.7.7/` |
| mount_ntfs 链接 | `/usr/local/sbin/mount_ntfs` → ntfs-3g |

## 硬盘信息

| 盘名 | 大小 | 格式 | 用途 |
|------|------|------|------|
| MyBook | 999.5 GB | NTFS | 备份盘 |
| Beanut | 4 TB | NTFS | 大容量存储（暂无操作） |

## 已卸载的冲突驱动

- Hasleo NTFS for Mac — 已卸载
- Paragon NTFS for Mac — 已卸载

## 挂载命令

```bash
# 手动挂载 MyBook
sudo diskutil unmount /dev/diskXsY
sudo /opt/homebrew/bin/ntfs-3g /dev/diskXsY /Volumes/MyBook -o allow_other,auto_xattr,local

# 验证
mount | grep MyBook  # 应显示 macfuse，非 read-only
```

## brew 国内源配置

```bash
# ~/.zshrc 中已配置
export HOMEBREW_BREW_GIT_REMOTE="https://mirrors.tuna.tsinghua.edu.cn/git/homebrew/brew.git"
export HOMEBREW_CORE_GIT_REMOTE="https://mirrors.tuna.tsinghua.edu.cn/git/homebrew/homebrew-core.git"
export HOMEBREW_BOTTLE_DOMAIN="https://mirrors.tuna.tsinghua.edu.cn/homebrew-bottles"
```

> 注意：`ntfs-3g-mac` 公式下载源是 `tuxera.com`，不走清华镜像，国内直连会卡。需要手动下载 `.tgz` 放入 brew 缓存。

## 脚本文件

| 文件 | 功能 |
|------|------|
| `mount_mybook.sh` | 自动查找 MyBook 并读写挂载 |
| `sync_mirror.sh` | 将 `~/Desktop/Beanut-Mirror/Mirror` 同步到 MyBook |

## 同步配置

| 源 | 目标 |
|----|------|
| `/Users/jinniu/Desktop/Beanut-Mirror/Mirror` | `/Volumes/MyBook/jinniu-Mirror` |

排除项：`.DS_Store`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `node_modules/`, `.codebuddy/`, `*.log`

## 关键经验

1. macOS 26 的 `/sbin/` 被 SIP 封印，不能替换 `mount_ntfs`，改放 `/usr/local/sbin/`
2. macFUSE 安装后需在隐私与安全性中授权 "Benjamin Fleischer" 系统扩展
3. 磁盘设备号（disk4/disk5/disk6）每次重插会变化
4. ntfs-3g 挂载不会在 Finder 侧边栏显示为外置磁盘，需通过 `/Volumes/MyBook/` 访问
5. `.codebuddy/` 文件夹不能删除，是项目相关数据

## 备注

- 安装包 `macfuse-5.3.3.dmg` 和 `ntfs-3g_ntfsprogs-2026.7.7.tgz` 保留在 `packages/` 目录下备用
- 所有操作与 AlphaMatrix 工程无关
- 后续此话题在此目录下讨论
